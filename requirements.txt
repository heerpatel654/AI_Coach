# AI Pronunciation Coach - Python dependencies
# Install with: pip install -r requirements.txt
#
# NOTE: ffmpeg is also required, but it is a system package, not a
# Python package, so it is NOT in this file. See README.md for
# install instructions per OS.

Flask==3.1.3
Werkzeug==3.1.5
Jinja2==3.1.6
MarkupSafe==3.0.3
itsdangerous==2.2.0
click==8.1.8
blinker==1.9.0

SpeechRecognition==3.17.0
pydub==0.25.1
gTTS==2.5.4
eng_to_ipa==0.0.2
pyphen==0.17.2

requests==2.32.5
